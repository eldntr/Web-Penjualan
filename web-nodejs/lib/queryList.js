const queryList = {
    // SELECT LIST
    getUsers: 'SELECT * FROM users',
    getProducts: 'SELECT * FROM products',

    // INSERT LIST
    registerUser: 'INSERT INTO pelanggan (pelanggan_id, pelanggan_nama, pelanggan_alamat) VALUES (?, ?,?)'
};

module.exports = queryList;

module.exports = queryList;